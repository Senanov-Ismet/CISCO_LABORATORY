#include <иопоток>

использование пространства имен std;

основной ()
{

	интервал IP1;
	инт IP2;
	инт IP3;
	интервал IP4;
	cout << "Введите первую часть вашего IP-адреса: ";
	cin >> ip1; cout << "\n";
	cout << "Введите вторую часть вашего IP-адреса: ";
	cin >> ip2; cout << "\n";
	cout << "Введите третью часть вашего IP-адреса: ";
	cin >> ip3; cout << "\n";
	cout << "Введите четвертую часть вашего IP-адреса: ";
	цин >> ip4; cout << "\n";

	если (ip1 <= 255 && ip2 <= 255 && ip3 <= 255 && ip4 <= 255)
	{
		cout << "IP-адрес: " << ip1 << "." << ip2 << "." << ip3 << "." << IP4;
	}
	еще
	{
		cout << "Что-то пошло не так... Ваше значение больше 255";
	}

	вернуть 0;
}

#include <иопоток>
#include <stdio.h>
#include <math.h>

использование пространства имен std;

основной ()
{
	int c0, количество = 0;
	cout << "Введите положительное число:";
	цин >> с0;
	если (с0 > 0)
	{
		пока (с0 > 1)
		{
			если (c0 % 2 == 0)
			{
				с0 /= 2;
			}
			еще
			{
				с0 = 3 * с0 + 1;
			}
			printf("%d\n", c0);
			количество++;
		}
		printf("шагов = %d", количество);
	}
	еще
	{
		printf("Ваше число отрицательное");
	}



	вернуть 0;
}

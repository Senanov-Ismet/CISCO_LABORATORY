#include <иопоток>
#include <stdio.h>
#include <math.h>

использование пространства имен std;

основной ()
{

	инт н;
	cout << "Введите положительное значение больше 1: ";
	цин >> п;
	если (n > 1 && n <= 30)
	{
		cout << '+';
		для (int i = 0; i < n; i++)
			cout << '_';
		cout << '+' << endl;
		для (int i = 0; i < n; i++) {
			cout << '|';
			для (int j = 0; j < n; j++)
				cout << ' ';
			cout << '|' << конец;
		}
		cout << '+';
		для (int i = 0; i < n; i++)
			cout << '_';
		cout << '+' << endl;
	}
	иначе, если (n > 30)
	{
		cout << "Извините, у вас слишком большое число :(";
	}
	еще
	{
		cout << "Вы ввели неверное значение";
	}


	вернуть 0;
}
